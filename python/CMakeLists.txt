# Python frontend (see agent/ARCHITECTURE.md).
# Install editable:  pip install -e ".[dev]"  from repository root.
message(STATUS "Python frontend: package under python/spacemissionplanner (notebooks/, tests/)")
