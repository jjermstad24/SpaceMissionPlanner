# Python environment for SpaceMissionPlanner (matches pyproject.toml + build tools).
# Used by configure.py; install manually with: pip install -r requirements.txt

numpy>=1.24
pytest>=7.4
PySide6>=6.6
pyvista>=0.43
pyvistaqt>=0.11
setuptools>=69
wheel

# Editable local package (python/spacemissionplanner)
-e .
